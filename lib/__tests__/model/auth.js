// import Auth from 'model/auth'

// const email = 'fake@email.com'
// const password = 'passwords_should_be_like_vacations_the_longer_the_better'

describe('testing the "Auth" class', () => {
  it.todo('Fix me')
  /*
  it('should create a valid instance of "Auth"', () => {
    const userAuth = new Auth({ email, password })
    expect(userAuth).toBeInstanceOf(Auth)
  })

  it('should throw an error on missing sign in information', async () => {
    const userAuth = new Auth({})
    await expect(userAuth.signIn()).rejects.toThrow(
      'Email and password are required to sign in',
    )
  })

  it('should throw an error on missing recover information', async () => {
    const userAuth = new Auth({})
    await expect(userAuth.sendPasswordResetEmail()).rejects.toThrow(
      'Email is required to recover password',
    )
  })

  it('should throw an error on missing register information', async () => {
    const userAuth = new Auth({})
    await expect(userAuth.createUser()).rejects.toThrow(
      'Email and password are required to register',
    )
  })
  */
})

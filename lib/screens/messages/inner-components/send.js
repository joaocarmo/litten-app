/**
 * @format
 * @flow
 */

import { Send as GFSend } from 'react-native-gifted-chat'

const Send: (args: any) => React$Node = (props) => (
  <GFSend {...props} containerStyle={{}} />
)

export default Send

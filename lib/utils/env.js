/**
 * @format
 * @flow
 */

import {
  IS_BETA_RELASE as IS_BETA_RELASE_STR,
  WEB_APP_BASE as WEB_APP_BASE_STR,
} from '@env'

export const IS_BETA_RELASE = IS_BETA_RELASE_STR === 'true'

export const WEB_APP_BASE = WEB_APP_BASE_STR
